# Graph RAG Pipeline — GraphDB, Local LLM & RDF
# Python 3.10+

# Core (chatbot + GraphDB)
requests>=2.28.0
python-dotenv>=1.0.0

# Evaluation (semantic similarity + LLM-as-judge)
langchain-openai>=0.1.0
openai>=1.0.0
numpy>=1.24.0

# Optional: RAGAS evaluation (evaluation/evaluate.py, add_ragas_scores.py)
# Uncomment if you run RAGAS:
# ragas>=0.1.0
# datasets>=2.14.0
